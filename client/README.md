# Клиентская часть

### Установить зависимости: `npm install`

### Для старта использовать `npm run` с парамтерами:
- `dev` - запуск в dev-режиме
- `build` - сборка проекта
- `start` - запуск собранного проекта

### Заполнить .env:

В ```.env``` введите ```NEXT_PUBLIC_API``` - эндпоинт API
